# Speech Emotion Recognition — Python Dependencies
# Install with:  pip install -r requirements.txt
#
# Python 3.11+ required.
# Note: TensorFlow 2.16.1 has limited compatibility with Python 3.14.
# For Python 3.14, use a modern TensorFlow version or consider alternatives.

# ── Deep learning ──────────────────────────────────────────────────────────
tensorflow==2.16.1

# ── Audio processing ───────────────────────────────────────────────────────
librosa>=0.10.0
soundfile>=0.12.0          # librosa's preferred backend for WAV decoding

# ── Numerical computing ────────────────────────────────────────────────────
numpy>=1.24.0

# ── Data handling ──────────────────────────────────────────────────────────
pandas>=2.0.0

# ── Machine learning utilities ─────────────────────────────────────────────
scikit-learn>=1.3.0        # LabelEncoder, train_test_split, metrics
joblib>=1.3.0              # Optional utility used by scikit-learn and related workflows

# ── Visualisation ──────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0            # Confusion matrix heatmap in evaluate.py
